apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "infra-eye.fullname" . }}-test-connection"
  labels:
    {{- include "infra-eye.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "infra-eye.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
