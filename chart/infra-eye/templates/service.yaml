apiVersion: v1
kind: Service
metadata:
  name: {{ include "infra-eye.fullname" . }}
  labels:
    {{- include "infra-eye.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "infra-eye.selectorLabels" . | nindent 4 }}
